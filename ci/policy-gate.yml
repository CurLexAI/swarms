name: policy-gate
on: pull_request
permissions: { contents: read }
jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "PR changing registry/policies must include ADR + sandbox tests + Mihwar approval"
