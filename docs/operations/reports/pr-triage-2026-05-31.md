# PR Triage Report — 2026-05-31

VERIFIED:
- GitHub API query for `CurLexAI/swarms` open pull requests returned one open PR at review time: PR #299, `Document GitHub webhook secret recovery and restore runtime-policy checks`.
- PR #299 changes documentation, `package.json`, runtime-policy TypeScript checks, and reports that aggregate `npm run check` is blocked by ADR-0001 boundary drift from forbidden `src/core` and `src/providers` paths.
- Local repository discovery before this fix found `src/core` and `src/providers` directories present, which are forbidden by `scripts/commander/adr-0001-boundary-gate.sh`.
- Local `.github/agents` now contains only the four canonical Copilot custom-agent profiles using the required `.agent.md` suffix.
- Local Render workflows are split into a no-secrets `render-preflight.yml` and a manual `render-deploy.yml` gated by `workflow_dispatch`, production environment, and `confirm_manual_gated_deploy == DEPLOY`.

CHANGED:
- Moved sovereign Python router/provider code out of forbidden `src/core` and `src/providers` into `src/policy/sovereign` and updated imports.
- Moved the TypeScript provider router out of `src/policy/provider-router.ts` into `.agents/router/provider-router.ts` so runtime/provider router code does not live in forbidden product-like source paths.
- Repaired `package.json`, `scripts/check-runtime-policy.ts`, and `src/policy/runtime-policy.ts` so runtime policy checks are parseable and fail closed.
- Renamed Copilot custom-agent profiles to `.github/agents/*.agent.md` and removed the duplicate non-agent-suffix supervisor profile.
- Renamed `kamikaze-recovery-supervisor` assets to `recovery-supervisor` and constrained the registry/prompt to no-secrets, read-only/propose-only governance.
- Rewrote Render preflight/deploy workflows to remove duplicated YAML fragments and preserve no-secrets/manual deployment separation.

VALIDATION:
- Required checks to run before merge:
  - `bash scripts/commander/modal-boundary-gate.sh .`
  - `bash scripts/commander/adr-0001-boundary-gate.sh .`
  - `npm run check --if-present`
  - `npm test --if-present`

RISKS:
- PR #299 is WAIT until it is rebased or reconciled with this boundary-drift fix because its body reports the same ADR-0001 blocker.
- Live Modal, Render production deployment, and endpoint smoke remain UNVERIFIED because this task intentionally did not deploy or call private runtime endpoints.
- GitHub issue/PR mutation was not performed; no PRs were merged or closed automatically.

DECISION:

| Request / PR theme | Decision | Reason |
| --- | --- | --- |
| Add custom agents configuration | GO conditional | Keep exactly one PR and only `.github/agents/bayyinah.agent.md`, `.github/agents/mihwar.agent.md`, `.github/agents/qarar-platform-supervisor.agent.md`, `.github/agents/free-birds.agent.md`. |
| Create internal agent markdown files | WAIT / Duplicate | Duplicate of custom agents; consolidate into the single `.github/agents/*.agent.md` PR. |
| Create custom agents using profiles | WAIT / Duplicate | Duplicate of custom agents; do not open or merge multiple PRs for the same profiles. |
| Add Kamikaze Recovery Supervisor | NO-GO as named | Renamed locally to `recovery-supervisor`; governance must remain no-secrets, read-only/propose-only, human-review-required. |
| Fix Render deploy reliability with no-secrets/manual split | GO conditional | Accept only one PR with `render-preflight.yml` no-secrets and `render-deploy.yml` manual production-gated deploy. |
| Fix Render deploy process for reliability | WAIT / Duplicate | Duplicate of the Render split; keep one PR only. |
| Update package.json scripts | WAIT | Allowed only when JSON is valid, tests are not weakened, and scripts point to existing checks. |
| Update GitHub Actions workflow YAML | WAIT | Title alone is insufficient; any deploy, secrets, or gate changes need strict diff review. |
| Add tests for runtime policy | GO conditional | Good after canonical policy source is settled and tests avoid forbidden `src/core` / `src/providers`. |
| Create runtime and provider router | NO-GO in forbidden paths | Router/provider implementation must not be under `src/core` or `src/providers`; use `.agents/router` and/or approved policy paths. |
| PR #299: Document GitHub webhook secret recovery and restore runtime-policy checks | WAIT | Useful runbook/runtime-policy repair, but it reports failing aggregate check from boundary drift and overlaps package/runtime-policy fixes. Rebase/reconcile after this boundary fix. |

NEXT ACTION:
1. Open or update the PR that introduced `src/core` / `src/providers` and require moving those files or closing that PR.
2. Rebase PR #299 or split it so webhook documentation remains separate from boundary/runtime-policy repairs.
3. Keep only one Copilot custom-agent PR and one Render no-secrets/manual deploy PR.
4. Defer Modal activation until boundary gates and aggregate checks are green, then record `UNVERIFIED_SECRET_MISSING` or `VERIFIED_ENDPOINT_SMOKE` based on actual smoke evidence.
