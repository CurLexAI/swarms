pyyaml>=6.0,<7.0
pytest>=8.0,<9.0
requests>=2.31,<3.0
modal>=0.64,<1.0
# POC deps — ingest_test.py (ADR-0001 sanctioned exception, 2026-05-16)
qdrant-client>=1.9,<2.0   # only Python client for Qdrant; no stdlib alternative
FlagEmbedding>=1.2,<2.0   # bge-m3 runner; sentence-transformers lacks bge-m3 colbert support
httpx>=0.27,<1.0   # local-only Ollama/llama.cpp HTTP adapters
pydantic>=2.0,<3.0   # typed models for providers, router, and SAMA ingestion
minio>=7.2,<8.0   # MinIO object-store adapter for SAMA ingestion
beautifulsoup4>=4.12,<5.0   # HTML extraction for SAMA parser
pdfplumber>=0.11,<1.0   # PDF extraction for SAMA parser
