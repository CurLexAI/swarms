services:
  mihwar-api:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - ./:/app
    command: sh -c "corepack enable && pnpm install && pnpm --filter @poc/api dev"
    ports:
      - "3000:3000"
